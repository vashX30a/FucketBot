How to make it work:

Make sure telnet service is enabled.
Compile fucketBot.erl and keywords.erl in Erlang shell 
ex: c(fucketBot).
Run fucketBot:start().

In command prompt:
enter command "telnet <ip address> <port>"